# Done For You Visuals — Site

Static multi-page portfolio + service proposal site for DFYV.

## Local preview
Open `index.html` in a browser, or:

```bash
npx serve .
```

## Deploy (Vercel)
- Framework preset: Other
- Root directory: `.`
- Build command: none
- Output directory: `.`
